# Discussion Question: Active Record and SQL Review 

### How to Run 

1. Clone the repository 
2. Run `bundle install` followed by `rake db:migrate`
3. Run `rake db:seed` to seed the database
4. Run `ruby run.rb`. You will hit a Pry. There, you can type Active Record commands and the corresponding SQL command will show up!